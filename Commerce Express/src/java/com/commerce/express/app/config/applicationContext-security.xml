<!-- SECTION A -->
<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans 
                    http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
                    http://www.springframework.org/schema/security 
                    http://www.springframework.org/schema/security/spring-security-3.1.xsd">
    <!-- SECTION A -->

    <!-- SECTION B -->
    <global-method-security secured-annotations="enabled"> </global-method-security>
    <!-- SECTION B -->

    <!-- SECTION C -->
    <http auto-config="true" use-expressions="true">
        <intercept-url pattern="/member**" access="hasRole('ROLE_CUSTOMER')"/>
        <form-login login-page="/login" 
                    default-target-url="/memberhome"
                    authentication-failure-url="/loginfailed"
        />
        <logout logout-success-url="/logout"/>
    </http>
    <!-- SECTION C -->

    <!-- SECTION D -->
    <authentication-manager alias="authenticationManager">
        <authentication-provider>
            <password-encoder hash="md5"/> 
            <jdbc-user-service 
                data-source-ref="dataSource"
                users-by-username-query="SELECT username, password, enabled FROM AccessDetails u WHERE u.username=?"
                authorities-by-username-query="SELECT u.username, r.rolename FROM AccessDetails u, Roles r WHERE u.id = r.user_id AND u.username=?"
            />
        </authentication-provider>
    </authentication-manager>
    <!-- SECTION D -->
</beans:beans> 